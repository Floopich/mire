services:
  mire:
    image: ghcr.io/${GH_OWNER:?lancer ./scripts/site.sh start <site>, il renseigne .env}/mire:latest
    # build: .          # decommenter pour construire localement au lieu de tirer l'image
    container_name: mire-${SITE:?definir SITE dans .env, ou lancer ./scripts/site.sh start <site>}
    restart: unless-stopped
    ports:
      - "8765:8765"
    volumes:
      - ./sites/${SITE}/data:/data
    cap_add:
      - NET_RAW
    environment:
      - TZ=${TZ:-Europe/Brussels}
      - BOOKED_DOWNLOAD=${BOOKED_DOWNLOAD:-}
      - BOOKED_UPLOAD=${BOOKED_UPLOAD:-}
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
